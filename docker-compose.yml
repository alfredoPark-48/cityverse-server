services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./frontend:/app/frontend
    environment:
      - GRID_FILE=data/2022_base.txt
      - TICKS_PER_SECOND=10
    restart: on-failure
